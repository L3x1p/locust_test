# Пример: PostgreSQL в Docker на порту 5433 (пароль postgres: Qqwerty1! → в URL ! = %21)
database_url=postgresql+psycopg2://postgres:Qqwerty1%21@localhost:5433/chat

# vLLM (у вас на 8000 с ключом local-token)
vllm_base_url=http://localhost:8000
vllm_api_key=local-token
